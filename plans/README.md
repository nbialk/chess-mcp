# Implementation Plans

Generated by the improve skill on 2026-06-11, against commit `ded809d`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title                                                        | Priority | Effort | Depends on   | Status |
|------|--------------------------------------------------------------|----------|--------|--------------|--------|
| 001  | Harden chess.com fetches + soften not-found `isError`        | P1       | S      | —            | TODO   |
| 002  | Move board replay positions out of model context (`_meta`)   | P1       | S      | — (after 001 preferred) | TODO |
| 003  | Vitest baseline + tests for pure helpers                     | P2       | M      | —            | TODO   |
| 004  | Extract typed chess.com client, remove `any`                 | P2       | M      | 003 (hard); 001, 002 (ordering) | TODO |
| 005  | Rewrite AGENTS.md with accurate agent guidance               | P3       | S      | — (content adapts to 003/006) | TODO |
| 006  | ESLint gate + `typecheck` script                             | P3       | S      | 004 (hard)   | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **001 → 002 → 004** all modify `src/server.ts`. 002 and 004 carry explicit
  "expected drift" allowances for their predecessors; executing out of order
  multiplies merge friction but is not forbidden for 001/002.
- **004 requires 003**: the extraction is only safe with the test harness in
  place, and 004's new pure functions get tests modeled on 003's file.
- **006 requires 004**: the lint preset errors on `no-explicit-any`, and the
  `any` usages live in the code 004 removes. Running 006 first would force
  rule downgrades.
- **005** is independent; two lines of its AGENTS.md content are included
  only if the scripts from 003/006 exist at execution time (handled inside
  the plan).

## Findings considered and rejected

- **"CI has no typecheck gate"** — initially reported, then disproven:
  `skybridge build` runs `tsc -b --force` before Vite
  (`node_modules/skybridge/dist/commands/build.js:21-29`), so `pnpm build`
  in CI already gates type errors. Only the lint gap survived (plan 006).
- **PostHog personal API key (`phx_…`) in `opencode.json` / `.mcp.json`** —
  both files are gitignored (`.gitignore:16-17`) and have zero git history;
  local-only MCP auth config, by design. Optional hardening: opencode
  supports `{env:VAR}` interpolation. No rotation needed.
- **SSRF via chess.com archive URLs** (`src/server.ts:250`) — URLs come from
  chess.com's own API response, not user input. By design.
- **`posthog-node` `flushAt: 1, flushInterval: 0`** — intentional for
  scale-to-zero delivery on Cloud Run; shutdown flush exists.
- **SIGTERM handler racing in-flight requests** (`src/server.ts:425-429`) —
  theoretical request drops on deploys; flush is ~ms and Cloud Run grants a
  grace period. Not worth fixing at current traffic.
- **Dependency audit** — `pnpm audit --prod` clean; no major-version lag on
  critical paths.
- **Formatter (Prettier/Biome format)** — deliberately excluded from plan
  006 to avoid whole-repo churn; revisit with more contributors.
- **Stale local build in `dist/`** — gitignored artifact; CI builds fresh.

## Direction options (surfaced, not planned)

Grounded options for the maintainer; ask for a design/spike plan if wanted:

- **`get-recent-games` tool** — the monthly archive is already fetched but
  only the last game is used (`src/server.ts:244-252`); replay UI is
  reusable per game.
- **`get-daily-puzzle` tool** — chess.com pub API exposes `/pub/puzzle`, and
  `Board` (`src/views/get-last-game/board.tsx:35`) already renders arbitrary
  FENs.
